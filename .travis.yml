language: c
sudo: required

install:
  - sudo apt-get update -qq
  - sudo apt-get install ia32-libs ia32-libs-multiarch -qq

before_script:
  - chmod a+x ndk.sh
  - ./ndk.sh
  - ./android-configure android-ndk-r10e
  
script: make
